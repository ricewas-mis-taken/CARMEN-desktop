__pycache__/
*.pyc
venv/
build/
dist/
*.spec

# All real user data (calendar.db, tasks.json, board.json, session state/
# history, config.json, calendar_errors.log, .env, review/board photo
# attachments) lives here now -- see each store module's *_PATH/PHOTOS_DIR
# constant. Never committed.
private/
